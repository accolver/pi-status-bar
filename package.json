{
  "name": "@accolver/pi-status-bar",
  "version": "0.1.1",
  "description": "Theme-aware Pi status bar extension with git state and AI-generated session titles.",
  "license": "MIT",
  "type": "module",
  "author": "Alan Colver",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/accolver/pi-status-bar.git"
  },
  "bugs": {
    "url": "https://github.com/accolver/pi-status-bar/issues"
  },
  "homepage": "https://github.com/accolver/pi-status-bar#readme",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi",
    "status-bar",
    "session-summary",
    "git",
    "tui"
  ],
  "files": [
    "extensions",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./extensions/status-bar.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": ">=0.82.1",
    "@earendil-works/pi-coding-agent": ">=0.82.1"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": {
      "optional": true
    },
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "check": "tsc --noEmit --skipLibCheck --module NodeNext --moduleResolution NodeNext --target ES2022 --strict extensions/status-bar.ts",
    "test": "bun test"
  }
}
